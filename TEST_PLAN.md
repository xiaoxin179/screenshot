# Screenshot Tool 测试计划

## 自动化/静态检查
- `dotnet build ScreenshotTool/ScreenshotTool.csproj -c Release`
- 确认 x64 Release 输出包含可执行文件。

## 手工验收
1. 启动程序，确认任务栏托盘出现图标，主窗口不显示。
2. 按 `Alt+Shift+S`，确认截图后出现“已复制到剪贴板”提示。
3. 在记事工具、浏览器或聊天窗口按 `Ctrl+V`，确认能粘贴 PNG。
4. 右键托盘开启“开机启动”，注销/登录后确认程序自动常驻。
5. 再次启动程序，确认不会出现第二个托盘图标。
6. 双屏/125%或150%缩放下截图，确认图片尺寸和内容正确。
7. 剪贴板被占用时，确认程序重试并给出失败提示而不崩溃。

## 已知 MVP 边界
- 当前热键直接复制全屏截图；选区、标注、保存对话框作为下一迭代。
