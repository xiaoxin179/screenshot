# 新增截图工具：实现与测试

## 范围

仅实现用户确认的 1～13 项，不包含 OCR、翻译、AI 打码、二维码或其他扩展。
保持已有暖白工具栏配色；使用文字按钮明确区分两种马赛克，绿色勾选按钮用于完成并复制。

| 编号 | 能力 | 使用方法 |
| --- | --- | --- |
| 1 | 椭圆、正圆 | 选择椭圆后拖动；Shift 约束正圆 |
| 2 | 直线 | 拖动绘制；Shift 约束水平或垂直 |
| 3 | 箭头 | 起点拖向目标位置 |
| 4 | 文字 | 点击位置输入，Enter 换行、Ctrl+Enter 提交；也可点击其他工具提交；Esc 放弃当前文字 |
| 5 | 样式 | 选择颜色、线宽、字号，设置保存在当前用户注册表 |
| 6 | 框选马赛克 | 在选区内拖出矩形 |
| 7 | 画笔马赛克 | 在选区内拖动涂抹，粗细滑块控制覆盖宽度 |
| 8 | 高亮 | 半透明笔画，不替换底图 |
| 9 | 撤销、重做 | 工具栏按钮或 Ctrl+Z、Ctrl+Y；文字编辑器内仍使用文本自身的撤销 |
| 10 | 保存 | 保存按钮或 Ctrl+S；PNG/JPG；保存后保留编辑窗口 |
| 11 | 标签、序号 | 点击位置，输入可选说明后 Ctrl+Enter；留空提交则只显示序号；自动编号 |
| 12 | 水印 | 设置字号、颜色、不透明度；点击位置输入水印，Ctrl+Enter 提交 |
| 13 | 手动滚动长截图 | 详见下一节 |

## 长截图使用及限制

1. 先框选一个**可滚动内容区域**，避开固定导航栏、悬浮控件、动画和视频。
2. 在添加标注前点击“长截图”，起始帧保留。
3. 手动向下滚动，每次不超过半屏；等待画面稳定后按 F8 或点击“采集下一帧”。
4. F9 或“结束采集”进入预览，可保存 PNG/JPG、复制或贴图。
5. 匹配失败时不追加图片，往回滚一点后重新采集；未滚动时不会重复追加。

这是传统像素匹配的向下手动拼接，不是自动滚动，也不支持向上追加或全应用无条件拼接。单张图限制为 30000 像素高、4000 万像素。拼接结果目前不再次进入标注窗口。

## 可重复执行的自动化测试

```powershell
dotnet run --project tests/AnnotationSmoke/AnnotationSmoke.csproj -c Release
# 先退出已有 Snaply，以免全局快捷键占用影响下一项：
dotnet run --project tests/StartupSmoke/StartupSmoke.csproj -c Release
```

- 各类标注输出改变像素、保留尺寸。
- 撤销精确还原原图、重做精确恢复、新笔画清空重做栈。
- Shift 正圆和直线约束。
- 画笔、高亮、马赛克单次点击不崩溃。
- 工具栏各模式分发、中文多行文本提交、撤销重做按钮分发。
- 相邻帧位移识别、重复帧/向上滚动/无关帧拒绝。
- 三帧拼接接缝逐像素相等；PNG 无损往返、JPG 尺寸保留。
- 隐藏启动注册快捷键、重复初始化、关闭设置保留快捷键。

## 本次桌面实测与待验收项

2026-09-24：标注/拼接/窗口测试 89 项通过；后台启动测试 5 项通过；自包含 win-x64 单文件发布成功。构建仍有项目原有的 WFAC010 DPI 配置提示，本次未改变 DPI 策略。

- 已实测：真实桌面框选、选区原色、椭圆绘制、工具切换、文字编辑框显示、完成按钮退出遮罩。
- 桌面自动化对模态窗口的键盘焦点识别有限；中文多行提交另由窗口级测试验证，真实中文输入法组合输入仍需手动验收。
- 长截图算法通过合成图逐像素测试；不同浏览器、聊天软件、缩放比例和固定栏场景需要逐应用验收，不宣称全部兼容。
- 手动检查保存取消/覆盖确认、剪贴板被占用、快速连续热键、跨屏不同 DPI、文本选区边缘换行和贴图滚轮缩放。
